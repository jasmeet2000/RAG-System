# ══════════════════════════════════════════════════════════════════════
# Development Dependencies — RAG System
# ══════════════════════════════════════════════════════════════════════
# Install: pip install -r requirements-dev.txt
# (Also installs production deps via the -r flag below)
# ══════════════════════════════════════════════════════════════════════

# Include production dependencies
-r requirements.txt

# ── Testing ─────────────────────────────────────────────────────────
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0      # Async test support for FastAPI
pytest-cov>=5.0,<6.0           # Coverage reporting
httpx>=0.27,<1.0               # TestClient uses httpx under the hood

# ── Linting & Formatting ───────────────────────────────────────────
ruff>=0.5,<1.0                 # Fast Python linter + formatter (replaces flake8, black, isort)
mypy>=1.10,<2.0                # Static type checking

# ── Development Tools ──────────────────────────────────────────────
ipython>=8.0                   # Better interactive Python shell
jupyter>=1.0                   # Jupyter notebooks for experiments
pre-commit>=3.0,<4.0           # Git hooks for automated checks
